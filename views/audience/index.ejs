<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Audience Segments</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
        color: #333;
        margin: 0;
        padding: 0;
      }

      h2 {
        text-align: center;
        color: #4caf50;
        margin-top: 30px;
      }

      a {
        color: #4caf50;
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      ul {
        list-style-type: none;
        padding: 0;
        margin: 20px auto;
        max-width: 800px;
      }

      li {
        background-color: #fff;
        padding: 15px;
        margin: 10px 0;
        border: 1px solid #ddd;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      li:hover {
        background-color: #f9f9f9;
      }

      .size {
        font-weight: bold;
        color: #777;
      }

      .view-link {
        color: #4caf50;
      }

      .view-link:hover {
        text-decoration: underline;
      }

      .back-link {
        display: block;
        text-align: center;
        margin-top: 20px;
        font-size: 16px;
      }
    </style>
  </head>
  <body>
    <h2>Audience Segments</h2>
    <a
      href="/audience/new"
      style="display: block; text-align: center; margin-bottom: 20px"
      >Create New Audience</a
    >
    <ul>
      <% audiences.forEach(audience => { %>
      <li>
        <div>
          <strong><%= audience.name %></strong>
          <span class="size">Size: <%= audience.size %></span>
        </div>
        <a class="view-link" href="/audience/<%= audience.audience_id %>"
          >View</a
        >
      </li>
      <% }) %>
    </ul>
    <a class="back-link" href="/">Back to Home</a>
  </body>
</html>
